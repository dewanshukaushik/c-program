#include<stdio.h>
#include<string.h>
#include <conio.h>
void main(){
    int temp;
    char str[30];
    printf("Enter ");
    gets(str);
    int count=0;
    while(1){
        if(s)
    }
    for(int i=0;i<count/2;i++)
    {
       temp= str[i];
       str[i]=str[count-i-1];
       str[count-i-1]=temp;
    }
    printf("%s",str);
}
